ArrayList<Integer> x_pos = new ArrayList<integer>();
ArrayList<Integer> y_pos = new ArrayList<integer>();
int hgt = 24, wdt = 24;
int block = 20;      // size of the window

void setup()
{
  size (500, 500);
  x_pos.add(4);   // starting postition of the snake
  y_pos.add(15);
}

void draw()
{
  background(0);
  fill(225);
  for (int i = 0; i < x_pos.size(); i++) {
    rect(x_pos.get
